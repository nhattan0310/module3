create database XayDungCSDL;
use  XayDungCSDL;

create table class(
`id` int not null,
`name` varchar(45)
);

create table teacher(
`id` int not null,
`name` varchar(45),
`age` int,
`country` varchar(45)
);


